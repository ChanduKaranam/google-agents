google-adk==2.6.0
google-genai>=2.14.0
# A2UI: native chips/cards/buttons in Gemini Enterprise (v0.8 basic catalog).
a2ui-agent-sdk==0.5.0
# [http-server] pulls in starlette + sse-starlette, required by
# A2AStarletteApplication at server startup (else ImportError in the lifespan).
a2a-sdk[http-server]==0.3.26
# Served over A2A (Cloud Run) — required for Gemini Enterprise to render A2UI.
uvicorn==0.34.0
# PDF summary tool (markdown -> styled A4 with syntax-highlighted code blocks).
fpdf2>=2.8.7
markdown>=3.5
pygments>=2.17
# Downscales uploaded images to a thumbnail data URI for the echoed image card.
pillow>=10.0.0
