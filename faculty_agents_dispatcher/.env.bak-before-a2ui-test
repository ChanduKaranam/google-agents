GOOGLE_GENAI_USE_ENTERPRISE=1
GOOGLE_CLOUD_PROJECT=supadha-dev
GOOGLE_CLOUD_LOCATION=us-central1

# Sethu API
SETHU_API_BASE_URL=https://sethu-dev-api.onrender.com/api/v1

# Gemini Enterprise authorization resource attached to this agent.
GE_AUTHORIZATION_ID=sethu-faculty

# Server-to-server secret. Keep it out of source control; on Agent Engine load
# it from Secret Manager rather than setting it here.
AGENT_AUTH_SECRET=rme6mDBgMYG6kxNi54OPV5jwCpJgUFHRlH77U2p2xKA=

# Local development only. Uncomment these AND add FACULTY_AGENT_ALLOW_DEV_AUTH=1
# to run outside Gemini Enterprise. Re-comment before deploying: Agent Engine/
# rejects empty env values, and a dev identity must never reach a deployment.
# A Google OAuth access token (ya29...) to exchange the normal way:
#FACULTY_AGENT_DEV_GOOGLE_ACCESS_TOKEN=
# Or a Sethu agent token, used as-is with no exchange. Paste a freshly minted
# one here when you need it; do not leave it behind after a debugging session.
#SETHU_DEV_AGENT_TOKEN=