google-adk[a2a]==2.6.1
# google-adk[a2a] pulls a2a-sdk but NOT its http-server extra, so sse-starlette
# is absent and A2AStarletteApplication raises at lifespan startup — not at
# import. The container dies with "failed to start and listen on port 8080"
# while every import test passes.
#
# Pin a2a-sdk: unconstrained it resolves to 1.x, an incompatible layout.
a2a-sdk[http-server]>=0.3,<0.4
google-cloud-aiplatform
requests
uvicorn
