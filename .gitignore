.venv/
__pycache__/
*.py[cod]

# adk deploy reads .env from the agent folder; it can hold project config and
# later secrets. Commit .env.example instead.
.env

# adk deploy agent_engine scratch folders
*_tmp[0-9]*/

# local adk session/artifact storage
.adk/
.claude/settings.local.json
# git worktrees created by tooling
.claude/worktrees/
